/** MikotoActor のコンパイル時評価をする。
 */
/**
 * Bugs:
 *   dmd2.060現在、CTFE のバグ(http://d.puremagic.com/issues/show_bug.cgi?id=6498) により実装不可能
 */
module sworks.mqo.ctmikoto;

import sworks.mqo.mikoto;

string loadMikoto( string filename )()
{

}